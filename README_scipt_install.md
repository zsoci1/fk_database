## Steps for automatic installation

1. **Copy project folder to Desktop**
   
2. **Open terminal**
   - `Command (⌘)` + `Space`: `Terminal`

3. **Navigate to folder**
   ```bash
   cd ~/Desktop/fitkitchen_database

4. **Run the scipt**
    chmod +x setup_and_build.sh
    ./setup_and_build.sh

5. **Run the app**
    double-click fitkitchen_database/dist/FitKitchen.app